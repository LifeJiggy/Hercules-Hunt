{
  "hooks": [
    {
      "event": "SessionStart",
      "command": "Write-Host '=== recon-ranker hook system initialized ===' -ForegroundColor Cyan"
    },
    {
      "event": "SessionStart",
      "command": "Write-Host '[RANKER] Attack surface ranking agent active. Model: claude-haiku-4-5-20251001. Input: recon output from recon-agent.' -ForegroundColor Green"
    },
    {
      "event": "SessionStart",
      "command": "Write-Host '[RANKER] Input files: subdomains.txt, live_hosts.txt, tech_stack.json, api_endpoints.txt, nuclei_raw.json, findings/secrets.log, headers.json, cookies.json.' -ForegroundColor Gray"
    },
    {
      "event": "SessionStart",
      "command": "Write-Host '[RANKER] Output: ranked_targets.json — each entry includes: url, priority_score (0-100), bug_classes[], tech_stack{}, nuclei_findings[], idor_likelihood (0-10), api_surface_score (0-10), feature_age_estimate, confidence, data_quality_flags{}.' -ForegroundColor Cyan"
    },
    {
      "event": "SessionStart",
      "command": "Write-Host '[RANKER] Scoring dimensions (5-axis): 1) IDOR likelihood (25%), 2) API surface richness (20%), 3) Tech stack Vuln match (20%), 4) Feature age/legacy (15%), 5) Nuclei/active scan findings (20%).' -ForegroundColor Magenta"
    },
    {
      "event": "SessionStart",
      "command": "Write-Host '[RANKER] Input validation: all input files must be non-empty. Missing inputs reduce confidence flag but do not block ranking. source_reliability tracked per file.' -ForegroundColor Yellow"
    },
    {
      "event": "Notification",
      "command": "Write-Host '[RANKER] Framework priority boosts: WordPress < 6.0 (+15), Drupal < 9 (+12), Django < 3.2 (+10), Rails < 6.1 (+10), Laravel < 9 (+12), Spring < 5.3 (+10), Express < 4.18 (+5), Next.js < 13 (+5).' -ForegroundColor Yellow"
    },
    {
      "event": "Notification",
      "command": "Write-Host '[RANKER] Numeric ID patterns in URLs: /users/{id}, /accounts/{id}, /orders/{id}, /documents/{id}, /invoices/{id}, /patients/{id} with sequential numeric IDs -> automatic IDOR priority boost +8.' -ForegroundColor Yellow"
    },
    {
      "event": "Notification",
      "command": "Write-Host '[RANKER] GraphQL endpoints (/graphql, /api/graphql, /v1/graphql) get +10 priority due to schema introspection potential exposing entire API surface.' -ForegroundColor Yellow"
    },
    {
      "event": "Notification",
      "command": "Write-Host '[RANKER] Subdomain pattern boosts: dev., staging., test., internal., admin., api., beta., lab. -> each +5. Dead SaaS CNAMEs -> +12 (subdomain takeover potential).' -ForegroundColor Yellow"
    },
    {
      "event": "Notification",
      "command": "Write-Host '[RANKER] Tech stack bonus: Node.js (+3 if old Express in package.json), PHP (+10 if WordPress/Drupal detected), Java (+5 if old Spring versions), Python (+8 if Flask/Django debug mode exposed).' -ForegroundColor Yellow"
    },
    {
      "event": "Notification",
      "command": "Write-Host '[RANKER] Auth endpoint bonus: /login, /signin, /oauth, /sso, /auth, /token endpoints -> +8 (auth bypass high reward). Password reset endpoints -> +6.' -ForegroundColor Yellow"
    },
    {
      "event": "Notification",
      "command": "Write-Host '[RANKER] Payment endpoint bonus: /payment, /checkout, /billing, /stripe, /paypal, /webhook -> +15 (high payout programs, sensitive functionality).' -ForegroundColor Yellow"
    },
    {
      "event": "Notification",
      "command": "Write-Host '[RANKER] S3/cloud storage bonus: s3.amazonaws.com, storage.googleapis.com, blob.core.windows.net, digitaloceanspaces.com -> +10 (public leak risk).' -ForegroundColor Yellow"
    },
    {
      "event": "Notification",
      "command": "Write-Host '[RANKER] Scoring weights confirm: IDOR likelihood (25%), API surface richness (20%), Tech stack Vuln match (20%), Feature age (15%), Nuclei findings (20%). Total = 100%.' -ForegroundColor Cyan"
    },
    {
      "event": "PreToolUse",
      "matcher": "Bash",
      "command": "Write-Host '[RANKER PRE] Verifying all recon input files exist and are non-empty before ranking computation...' -ForegroundColor DarkYellow"
    },
    {
      "event": "PreToolUse",
      "matcher": "Bash",
      "command": "if ($lastCommand -match 'cat subdomains|cat live_hosts|cat tech_stack|cat api_endpoints|cat nuclei') { Write-Host '[RANKER PRE] Recon input file read detected. Proceeding with ranking analysis.' -ForegroundColor DarkYellow }"
    },
    {
      "event": "PreToolUse",
      "matcher": "Bash",
      "command": "if ($lastCommand -match 'python.*rank|node.*rank|ranking|prioritize|sort') { Write-Host '[RANKER PRE] Ranking computation tool detected. Compute all 5 dimensions for comprehensive scoring.' -ForegroundColor DarkYellow }"
    },
    {
      "event": "PreToolUse",
      "matcher": "Bash",
      "command": "if ($lastCommand -match 'jq|json|sort|uniq|awk') { Write-Host '[RANKER PRE] Data processing tool detected. Use jq for JSON, sort/uniq/awk for text recon output.' -ForegroundColor DarkYellow }"
    },
    {
      "event": "PreToolUse",
      "matcher": "Bash",
      "command": "if ($lastCommand -match 'wc -l|Measure-Object|Get-Content.*Measure') { Write-Host '[RANKER PRE] Input file size/length check detected. Larger input = more ranking reliability. Track count metrics per input.' -ForegroundColor DarkYellow }"
    },
    {
      "event": "PreToolUse",
      "matcher": "Bash",
      "command": "if ($lastCommand -match 'grep.*id=|grep.*userId|grep.*accountId|grep.*numeric') { Write-Host '[RANKER PRE] IDOR pattern detection on input URLs. Flagging all URL paths containing numeric resource identifiers.' -ForegroundColor DarkYellow }"
    },
    {
      "event": "PreToolUse",
      "matcher": "Bash",
      "command": "if ($lastCommand -match 'grep.*/graphql|grep.*graphql|grep.*__schema') { Write-Host '[RANKER PRE] GraphQL endpoint detection. All GraphQL endpoints receive bonus ranking +10.' -ForegroundColor DarkYellow }"
    },
    {
      "event": "PreToolUse",
      "matcher": "Bash",
      "command": "if ($lastCommand -match 'jq.*score|jq.*priority|jq.*rank|awk.*score|python.*score') { Write-Host '[RANKER PRE] Score computation/aggregation detected. Ensure all 5 dimensions are computed and weighted correctly.' -ForegroundColor DarkYellow }"
    },
    {
      "event": "PreToolUse",
      "matcher": "Bash",
      "command": "if ($lastCommand -match 'cp|mv|copy|move.*ranked') { Write-Host '[RANKER PRE] Ranked output distribution detected. ranked_targets.json is the canonical output for downstream agents.' -ForegroundColor DarkYellow }"
    },
    {
      "event": "PostToolUse",
      "matcher": "Bash",
      "command": "Write-Host '[RANKER POST] Reviewing ranking computation and output integrity for downstream hunter consumption.' -ForegroundColor DarkCyan"
    },
    {
      "event": "PostToolUse",
      "matcher": "Bash",
      "command": "if ($lastOutput -match 'priority_score|score|rank|P1|P2|P3|Critical|High|Medium|Low') { Write-Host '[RANKER POST] Rankings generated. Verify priority distribution: aim for 20% P1, 40% P2, 30% P3, 10% INFO. Adjust if skewed.' -ForegroundColor Green }"
    },
    {
      "event": "PostToolUse",
      "matcher": "Bash",
      "command": "if ($lastOutput -match 'error|exception|file not found|null|undefined|NaN|empty|missing') { Write-Host '[RANKER POST] Ranking output issue detected. Review input files for completeness. Missing recon data => inaccurate ranking, lower confidence flag.' -ForegroundColor Red }"
    },
    {
      "event": "PostToolUse",
      "matcher": "Bash",
      "command": "if ($lastOutput -match 'IDOR|idor|user.id|account.id|order.id|document.id|patient.id|invoice.id|numeric') { Write-Host '[RANKER POST] IDOR candidates flagged in ranking. These are top P1 targets for p1-warrior. Prioritize for immediate hunting.' -ForegroundColor Yellow }"
    },
    {
      "event": "PostToolUse",
      "matcher": "Bash",
      "command": "if ($lastOutput -match 'GraphQL|graphql|/graphql|introspection|__schema|__type') { Write-Host '[RANKER POST] GraphQL endpoints ranked high. Verify introspection accessibility before hunter runs schema dump. If 403 at /graphql?query={__schema}, lower confidence.' -ForegroundColor Yellow }"
    },
    {
      "event": "PostToolUse",
      "matcher": "Bash",
      "command": "if ($lastOutput -match 'wordpress|drupal|joomla|django|rails|laravel|spring|express|next|nuxt|flask|fastapi') { Write-Host '[RANKER POST] Frameworks/tech matched. Each framework gets vulnerability pattern library lookup. WordPress -> plugin Vuln DB, Django -> CVE tracker.' -ForegroundColor Green }"
    },
    {
      "event": "PostToolUse",
      "matcher": "Bash",
      "command": "if ($lastOutput -match 'CVE|nuclei|matched|vulnerability|high|critical|medium|low') { Write-Host '[RANKER POST] Nuclei results incorporated. Auto-promote high/critical CVSS >= 7.0 to P1. Medium CVSS 4.0-6.9 stays P2.' -ForegroundColor Yellow }"
    },
    {
      "event": "PostToolUse",
      "matcher": "Bash",
      "command": "if ($lastOutput -match 'api|endpoint|rest|soap|grpc|swagger|openapi|swagger.json|openapi.json|\\.well-known/openapi') { Write-Host '[RANKER POST] API surface mapped. Most API-heavy apps get higher ranking due to authz complexity. REST > SOAP > GraphQL for surface volume.' -ForegroundColor Green }"
    },
    {
      "event": "PostToolUse",
      "matcher": "Bash",
      "command": "if ($lastOutput -match 'subdomain|tld|new.domain|dev\\.|staging\\.|test\\.|internal\\.|admin\\.|corp\\.|employee\\.') { Write-Host '[RANKER POST] Subdomain patterns analyzed. dev/staging/internal subdomains often have weaker auth, no WAF, debug enabled. Prioritize these.' -ForegroundColor Green }"
    },
    {
      "event": "PostToolUse",
      "matcher": "Bash",
      "command": "if ($lastOutput -match 'redirect|open.redirect|callback|oauth|sso|saml|ldap|saml2|openid') { Write-Host '[RANKER POST] Auth-related endpoints found. High priority: open redirect + OAuth chain potential. redirect_uri manipulation -> token theft.' -ForegroundColor Yellow }"
    },
    {
      "event": "PostToolUse",
      "matcher": "Bash",
      "command": "if ($lastOutput -match 's3|bucket|storage|blob|gcs|azure.blob|alibaba.oss|r2.cloudflare|cos') { Write-Host '[RANKER POST] Cloud storage/CDN endpoints found. Check for public ACLs, bucket listing, path traversal, CORS wildcards. +10 priority.' -ForegroundColor Green }"
    },
    {
      "event": "PostToolUse",
      "matcher": "Bash",
      "command": "if ($lastOutput -match 'webhook|callback|notify|integration|stripe|paypal|square|braintree|razorpay|mollie') { Write-Host '[RANKER POST] Payment/webhook endpoints found. High reward target. Test for signature bypass and replay. +15 priority.' -ForegroundColor Yellow }"
    },
    {
      "event": "PostToolUse",
      "matcher": "Bash",
      "command": "if ($lastOutput -match 'header|X-Forwarded|X-Real-IP|X-Original-URL|X-Rewrite-URL|X-Host') { Write-Host '[RANKER POST] Web server headers detected (proxy, CDN, WAF). These indicate backend architecture complexity and potential header injection targets.' -ForegroundColor Gray }"
    },
    {
      "event": "PostToolUse",
      "matcher": "Bash",
      "command": "if ($lastOutput -match 'cdn|cloudfront|fastly|akamai|azure.cdn|google.cloud.cdn| BunnyCDN|keycdn') { Write-Host '[RANKER POST] CDN detected. CDNs can cache sensitive responses, have edge-side includes, or misconfigured origin pull.' -ForegroundColor Gray }"
    },
    {
      "event": "PostToolUse",
      "matcher": "Bash",
      "command": "if ($lastOutput -match 'version|X-Powered-By|Server:|X-Generator|X-Version|X-Runtime') { Write-Host '[RANKER POST] Server/software version exposed. Version-specific CVEs can be matched. High value for exploit-researcher phase.' -ForegroundColor Gray }"
    },
    {
      "event": "PostToolUse",
      "matcher": "Bash",
      "command": "if ($lastOutput -match 'debug|trace|verbose|stack|traceback|error.*page|whoops|symfony.*debug|laravel.*debug') { Write-Host '[RANKER POST] Debug mode detected in production stack. +8 priority. Debug mode leaks: stack traces, env vars, DB queries, internal paths.' -ForegroundColor Red }"
    },
    {
      "event": "PostToolUse",
      "matcher": "Bash",
      "command": "if ($lastOutput -match 'cors|Access-Control-Allow|Access-Control-Allow-Origin|Access-Control-Allow-Credentials') { Write-Host '[RANKER POST] CORS configuration found. Wildcard + Credentials = critical authz bug. Check for origin reflection in ranking.' -ForegroundColor Yellow }"
    },
    {
      "event": "PostToolUse",
      "matcher": "Bash",
      "command": "if ($lastOutput -match 'cookie|sessionid|PHPSESSID|JSESSIONID|connect.sid|remember_me|refresh_token|access_token') { Write-Host '[RANKER POST] Session management identified. Weak session tokens = session fixation/broken auth risk. Track for p1-warrior auth testing.' -ForegroundColor Gray }"
    },
    {
      "event": "PostToolUse",
      "matcher": "Bash",
      "command": "if ($lastOutput -match 'oauth|openid|saml|sso|keycloak|auth0|okta|ping|cognito|azure.ad|google.identity') { Write-Host '[RANKER POST] Identity provider detected. OAuth misconfig common: redirect_uri validation, scope escalation, token leakage via referrer/URL.' -ForegroundColor Yellow }"
    },
    {
      "event": "PostToolUse",
      "matcher": "Bash",
      "command": "if ($lastOutput -match 'kubernetes|docker|k8s|apache|nginx|traefik|envoy|istio|linkerd') { Write-Host '[RANKER POST] Infrastructure/container detected. K8s API server, docker socket, sidecar RCE are potential high-impact bugs if exposed.' -ForegroundColor Yellow }"
    },
    {
      "event": "PostToolUse",
      "matcher": "Bash",
      "command": "if ($lastOutput -match 'ws://|wss://|socket|realtime|pusher|ably|firebase|eventstream') { Write-Host '[RANKER POST] WebSocket/realtime endpoint found. WebSocket authz often weaker than REST. IDOR via socket messages common.' -ForegroundColor Yellow }"
    },
    {
      "event": "PostToolUse",
      "matcher": "Bash",
      "command": "if ($lastOutput -match 'internal|intranet|local|localhost|127.0.0.1|10\\.|172\\.16|192.168|metadata|169.254') { Write-Host '[RANKER POST] Internal/private IP patterns found. Potential SSRF target if web app makes outbound requests. +6 priority for SSRF hunting.' -ForegroundColor Yellow }"
    },
    {
      "event": "PostToolUse",
      "matcher": "Bash",
      "command": "if ($lastOutput -match 'admin|dashboard|manage|console|panel|control|setup|config|settings') { Write-Host '[RANKER POST] Admin/management endpoints found. These are prime auth bypass targets. Verify authz enforcement before hunter starts.' -ForegroundColor Yellow }"
    },
    {
      "event": "PostToolUse",
      "matcher": "Bash",
      "command": "if ($lastOutput -match 'health|status|ping|heartbeat|readiness|liveness|metrics|prometheus|grafana') { Write-Host '[RANKER POST] Operational endpoints found. /metrics may expose sensitive metrics (token counts, request rates). /status may leak internal IPs.' -ForegroundColor Gray }"
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Priority distribution guidance: soft cap P1 at 20% of total (hunter saturation), soft floor P3 at 20% (low-confidence targets only).' -ForegroundColor DarkGray"
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Confidence flag meanings: high = all 5 inputs present and verified, medium = 3-4 inputs present, low = 1-2 inputs only. low confidence targets get +2 in ranking as they may have hidden value.' -ForegroundColor DarkGray"
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Data quality flags: {complete: bool, stale: bool (older than 24h), optimistic: bool (inferred from partial data), needs_rerun: bool}. Stale inputs flag needs_rerun=true.' -ForegroundColor DarkGray"
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Output feed: p1-warrior receives top 20 targets with {url, bug_classes[], priority_score, confidence, suggested_attack_vectors[], tech_stack{}, nuclei_findings[]}.' -ForegroundColor DarkGray"
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Output feed: chain-builder receives targets with multiple bug class indicators for chaining analysis (e.g., IDOR + SSRF endpoint on same domain).' -ForegroundColor DarkGray"
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Output feed: js-analysis receives frontend URL seeds from ranked targets {url, tech_stack.framework} for targeted JS bundle discovery.' -ForegroundColor DarkGray"
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Output feed: autopilot receives task queue {target, phase, bug_classes[], priority, confidence, estimated_time_min}.' -ForegroundColor DarkGray"
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Output feed: exploit-researcher receives tech_stack matched vulnerabilities for CVE lookup and exploit PoC retrieval.' -ForegroundColor DarkGray"
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Tiebreaker logic: same priority_score -> prefer (1) lower confidence (more investigation needed), (2) older tech stack, (3) higher API surface, (4) more Nuclei matches, (5) subdomain dev/staging prefix.' -ForegroundColor DarkGray"
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Re-ranking trigger: if new recon data available (atomic=True) or if hunter requests re-rank after finding cluster. Re-rank is cheap: O(n log n) with cached data.' -ForegroundColor DarkGray"
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Output validation: ranker output must pass sanity checks: (1) no duplicate URLs, (2) all scores in 0-100 range, (3) all URLs in scope, (4) bug_classes subset of known classes, (5) confidence in {high,medium,low}.' -ForegroundColor DarkGray"
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Excluded from ranking: out-of-scope domains, URLs with only static assets (css, js, img, font), CDN-only domains, monitoring/health endpoints without logic.' -ForegroundColor DarkGray"
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Integration with recon-ranker agent prompt: this hooks file provides the runtime safety/net. Agent prompt defines the algorithm. Both must be read together for full understanding.' -ForegroundColor DarkGray"
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Audit trail: every ranking run logged to audit/ranker_audit.jsonl with {timestamp, target_domain, input_files_hash, output_target_count, top_5_urls, run_duration_ms}.' -ForegroundColor DarkGray"
    },
    {
      "event": "Stop",
      "command": "Write-Host '[RANKER STOP] Ranking complete. Final verification: (1) all recon inputs consumed, (2) all scores computed and in range 0-100, (3) ranked_targets.json written and non-empty, (4) top 20 targets flagged for hunter, (5) audit log written, (6) confidence flags set, (7) no out-of-scope URLs in output.' -ForegroundColor Yellow"
    },
    {
      "event": "Stop",
      "command": "Write-Host '[RANKER STOP] Next: /p1-warrior <target> receives ranked_targets.json and begins systematic hunting on top 20 targets. Maximum 10 min per bug class.' -ForegroundColor Green"
    },
    {
      "event": "SessionStart",
      "command": "Write-Host '[RANKER] Weighted scoring algorithm: idor_likelihood * 0.25 + api_surface_score * 0.20 + tech_stack_match_score * 0.20 + feature_age_score * 0.15 + nuclei_score * 0.20 = final_priority_score (0-100). Each sub-score computed independently.' -ForegroundColor Cyan"
    },
    {
      "event": "SessionStart",
      "command": "Write-Host '[RANKER] Confidence thresholds: high (all 5 input files present, all verified within 24h, zero OOS contacts), medium (3-4 files present, some verified), low (1-2 files only, unverified, >24h old). Low-confidence targets flagged for re-recon first.' -ForegroundColor Yellow"
    },
    {
      "event": "SessionStart",
      "command": "Write-Host '[RANKER] Feature age scoring: endpoints with no Last-Modified header = oldest tier (+15), Last-Modified > 3 years (+12), 1-3 years (+8), 6-12 months (+5), <6 months (+0). Old endpoints have accumulated more undocumented behavior.' -ForegroundColor Gray"
    },
    {
      "event": "SessionStart",
      "command": "Write-Host '[RANKER] Nuclei finding scoring: critical template match = +20 (auto-promote to P1), high = +12, medium = +5, low = +2, info = +0. Multiple templates matching same target = cumulative +5 bonus for finding diversity.' -ForegroundColor Gray"
    },
    {
      "event": "SessionStart",
      "command": "Write-Host '[RANKER] Subdomain environment mapping: prod-domain.com (score as-is), www.* (+0), api.* (+5), admin.* (+8), internal.* (+7), dev.* (+6), staging.* (+6), test.* (+4), lab.* (+5), beta.* (+5), employee.* (+8), corp.* (+8).' -ForegroundColor Gray"
    },
    {
      "event": "SessionStart",
      "command": "Write-Host '[RANKER] Endpoint type classification: auth endpoints (/login, /oauth, /token) = +8, admin endpoints (/admin, /manage, /dashboard) = +8, file operation endpoints (/upload, /export, /import) = +7, query endpoints (/search, /query, /filter) = +4, read-only endpoints (/view, /get, /list) = +2, health endpoints (/health, /status) = +0.' -ForegroundColor Gray"
    },
    {
      "event": "PreToolUse",
      "matcher": "Bash",
      "command": "if ($lastCommand -match 'curl.*nuclei|nuclei.*-target|nuclei.*-u|nuclei.*-l') { Write-Host '[RANKER PRE] Nuclei scanning against ranked targets detected. Running priority-ordered scan. Critical findings = immediate re-rank boost. Results feed back into nuclei_score dimension.' -ForegroundColor DarkYellow }"
    },
    {
      "event": "PreToolUse",
      "matcher": "Bash",
      "command": "if ($lastCommand -match 'naabu|masscan|nmap.*-p|port.*scan') { Write-Host '[RANKER PRE] Port scanner detected. Results enrich api_surface_score dimension. Open ports inform endpoint classification (80/443=web, 8080=alt http, 3306=MySQL exposed, 6379=Redis, 9200=Elasticsearch).' -ForegroundColor DarkYellow }"
    },
    {
      "event": "PreToolUse",
      "matcher": "Bash",
      "command": "if ($lastCommand -match 'wafw00f|wafdetect|whatwaf|identywaf') { Write-Host '[RANKER PRE] WAF detection tool. If WAF found, flag for p1-warrior: WAF bypass techniques needed. Some WAFs indicate mature security posture -> lower confidence. Missing WAF on sensitive targets = +5.' -ForegroundColor DarkYellow }"
    },
    {
      "event": "PreToolUse",
      "matcher": "Bash",
      "command": "if ($lastCommand -match 'shodan|shodan.*search|censys|zoomeye|fofa|hunter|binaryedge') { Write-Host '[RANKER PRE] External search engine API detected. Results enrich tech_stack and version detection. Document external data sources and their recency in data_quality_flags.' -ForegroundColor DarkYellow }"
    },
    {
      "event": "PreToolUse",
      "matcher": "Bash",
      "command": "if ($lastCommand -match 'httpx.*-tech|whatweb|wappalyzer|builtwith|retire|retire.js') { Write-Host '[RANKER PRE] Tech fingerprinting tool detected. Results feed tech_stack_match dimension. Version detection quality impacts CVE correlation accuracy. Document detection method and confidence per technology.' -ForegroundColor DarkYellow }"
    },
    {
      "event": "PreToolUse",
      "matcher": "Bash",
      "command": "if ($lastCommand -match 'waybackurls|commoncrawl|urlscan|otx|shodan.*crawler') { Write-Host '[RANKER PRE] Historical URL/endpoint discovery tool. Found endpoints may be old (feature age boost). Cross-reference with current live endpoints to identify stale but potentially vulnerable paths.' -ForegroundColor DarkYellow }"
    },
    {
      "event": "PostToolUse",
      "matcher": "Bash",
      "command": "if ($lastOutput -match 'X-Powered-By|X-Generator|X-Version|X-Runtime|X-AspNet-Version|X-AspNetMvc-Version') { Write-Host '[RANKER POST] Server/version header fingerprinting result. Exact version enables targeted CVE lookup. Cross-reference version with NVD for known exploits. Add to tech_stack{} with version_pinned=true flag.' -ForegroundColor Gray }"
    },
    {
      "event": "PostToolUse",
      "matcher": "Bash",
      "command": "if ($lastOutput -match 'Set-Cookie|session|token|jwt|csrf|XSRF-TOKEN|laravel_session|PHPSESSID|express:sid|connect.sid') { Write-Host '[RANKER POST] Session cookie analysis result. Document: cookie name, HttpOnly flag, Secure flag, SameSite setting, Domain scope, Path scope, Expiration. Weak session config = auth bypass risk.' -ForegroundColor Gray }"
    },
    {
      "event": "PostToolUse",
      "matcher": "Bash",
      "command": "if ($lastOutput -match 'reseller|partner|affiliate|marketplace|vendor|merchant|customer.portal') { Write-Host '[RANKER POST] Multi-tenant/multi-role architecture detected. Multi-tenant apps have high IDOR risk. Each tenant data isolation boundary = potential IDOR testing location. Flag for p1-warrior multi-account testing.' -ForegroundColor Yellow }"
    },
    {
      "event": "PostToolUse",
      "matcher": "Bash",
      "command": "if ($lastOutput -match 'graphql|playground|graphiql|voyager|altair|insomnia.*graphql') { Write-Host '[RANKER POST] GraphQL tooling/playground detected. Introspection usually enabled in dev/playground mode. Introspection = full schema = all attack surfaces visible. Auto-boost GraphQL targets +12.' -ForegroundColor Yellow }"
    },
    {
      "event": "PostToolUse",
      "matcher": "Bash",
      "command": "if ($lastOutput -match 'swagger|openapi|swagger-ui|swagger.json|openapi.json|redoc|rapidoc') { Write-Host '[RANKER POST] OpenAPI/Swagger documentation endpoint found. Full API surface revealed = complete attack surface map. Test for: (1) sensitive endpoints in spec, (2) deprecated endpoints, (3) internal-only endpoints exposed.' -ForegroundColor Yellow }"
    },
    {
      "event": "PostToolUse",
      "matcher": "Bash",
      "command": "if ($lastOutput -match 'elastic|kibana|grafana|prometheus|jaeger|zipkin|opentelemetry|apm|trace') { Write-Host '[RANKER POST] Observability/APM stack detected. Kibana/Grafana often have weak auth or default credentials. /metrics endpoint may expose internal service communication. +6 observability bonus.' -ForegroundColor Yellow }"
    },
    {
      "event": "PostToolUse",
      "matcher": "Bash",
      "command": "if ($lastOutput -match 'jenkins|gitlab|github.*enterprise|bitbucket|argo|argoCD|argo.*rollouts|spinnaker') { Write-Host '[RANKER POST] CI/CD pipeline system detected. These are high-value targets: pipeline secrets, build tokens, deployment credentials. CI/CD access = supply chain compromise. +10 priority.' -ForegroundColor Yellow }"
    },
    {
      "event": "PostToolUse",
      "matcher": "Bash",
      "command": "if ($lastOutput -match 'consul|etcd|zookeeper|nacos|eureka|vault|secret|credentials.*store') { Write-Host '[RANKER POST] Service discovery/secret management detected. Consul/etcd default ports 8500/2379. Vault default 8200. If exposed = critical info leak or RCE. +10 priority + immediate flag for security-reviewer.' -ForegroundColor Red }"
    },
    {
      "event": "PostToolUse",
      "matcher": "Bash",
      "command": "if ($lastOutput -match 'mailhog|mailtrap|sendgrid.*sandbox|mailgun.*sandbox|ses.*sandbox|smtp.*local|debug.*email') { Write-Host '[RANKER POST] Email debugging/staging service detected. May expose email contents to testers. Check for email enumeration, password reset token leakage, PII in emails. +5 priority.' -ForegroundColor Yellow }"
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Re-ranking after recon improvement: when p1-warrior finds new routes or tech details, re-rank that target immediately. New information = adjusted score. Hunter can trigger re-rank via /re-rank {target_url}.' -ForegroundColor DarkGray"
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Stale data handling: recon data older than 48h triggers needs_rerun=true flag. Ranker re-runs with fresh data before hunter deployment. Stale data reduces confidence by 1 tier (high->medium, medium->low).' -ForegroundColor DarkGray"
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Program-specific customization: each program can have custom scoring weights in config/programs/{program}/ranker_weights.json. Weight overrides this defaults. Load program profile at session start if exists.' -ForegroundColor DarkGray"
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Multi-target coordination: when ranking multiple programs, sort by (1) program bounty potential, (2) recon richness, (3) hunter confidence. Allocate parallel hunters per program. Session budget divided proportionally.' -ForegroundColor DarkGray"
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Intelligence enrichment: for each ranked target, enrich with: (1) Shodan/Censys host data, (2) Wayback Machine historical URLs, (3) GitHub code search for similar target, (4) CISA KEV for known compromised software. Enrichment adds context without direct probing.' -ForegroundColor DarkGray"
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Bug class targeting: map target characteristics to bug class probability. REST API + numeric IDs = IDOR high. Node.js app = prototype pollution. Java app = deserialization. PHP app = file inclusion. Cloud infra = SSRF. Logical mapping not random.' -ForegroundColor DarkGray"
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] WAF-affected ranking: if WAF detected on target, reduce automated scanner score (sqlmap, nuclei) by 30%, increase manual technique score by 20%. WAF targets need human creativity more than automated tools.' -ForegroundColor DarkGray"
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Target blacklisting: maintain config/blacklist.json for known false positives, out-of-scope leaked domains, honeypot indicators, de-prioritized targets. Blacklisted targets still ranked but at bottom with confidence=low.' -ForegroundColor DarkGray"
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Integration with report-writer: ranker output includes impact estimate based on target type (e-commerce=high, SaaS=high, gov=critical, personal blog=low). Report-writer uses this for initial CVSS temporal score estimation.' -ForegroundColor DarkGray"
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Integration with js-analysis: pass ranked target frontend URLs to js-analysis as priority seeds. JS analysis runs in parallel with hunter start. Results feed back to ranker for dynamic re-rank based on discovered secrets/endpoints.' -ForegroundColor DarkGray"
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Performance optimization: ranking algorithm runs in O(n log n) via heap-based top-k selection. For 10000 targets, top 20 extraction takes <100ms. Cache ranking for 1 hour. Recompute only on explicit re-rank or fresh recon data.' -ForegroundColor DarkGray"
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Cross-reference enrichment: each target enriched with (1) Wayback Machine URLs, (2) GitHub code search for similar apps, (3) CISA KEV cross-match, (4) Shodan host history, (5) crt.sh certificate history. Enrichment feeds into feature_age and tech_stack dimensions.' -ForegroundColor DarkGray"
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Target persona analysis: if target handles (1) financial data = boost IDOR/SSRF, (2) healthcare data = boost data exfil vectors, (3) PII = boost privacy-related bugs, (4) government = boost auth bypass, (5) gaming = boost business logic/cheat.' -ForegroundColor DarkGray"
    },
    {
      "event": "Stop",
      "command": "Write-Host '[RANKER STOP] Output files written: ranked_targets.json (canonical), ranked_targets_backup_{timestamp}.json (backup), ranker_audit.jsonl (audit), confidence_report.md (summary). All files signed with checksum in output/checksums.txt.' -ForegroundColor Green"
    },
    {
      "event": "SessionStart",
      "command": "Write-Host '[RANKER] Context7 integration: resolving framework/library docs at ranking time. Load framework-specific Vuln patterns from context7 for accurate tech_stack_match scoring. Each framework mapped to known Vuln classes and CVE IDs.' -ForegroundColor Cyan"
    },
    {
      "event": "SessionStart",
      "command": "Write-Host '[RANKER] Framework Vuln pattern library: WordPress (SQLi via WP_Meta_Query, XSS via comment field, auth bypass via REST API), Django (SSRF via URL validation bypass, template injection, debug mode exposure), Rails (mass assignment via unpermitted params, auth bypass via remember_me), Laravel (mass assignment, debug mode, SQL via raw queries).' -ForegroundColor Magenta"
    },
    {
      "event": "SessionStart",
      "command": "Write-Host '[RANKER] Endpoint pattern library for bug class matching: /api/users/{id} -> IDOR, /api/search?q= -> SQLi/NoSQLi, /api/upload -> file upload bugs, /proxy?url= -> SSRF, /admin -> auth bypass, /graphql -> introspection leak, /oauth/callback -> redirect / token theft.' -ForegroundColor Cyan"
    },
    {
      "event": "SessionStart",
      "command": "Write-Host '[RANKER] API richness scoring: REST with Swagger spec = +20, REST without spec = +10, GraphQL introspection enabled = +15, GraphQL disabled = +5, SOAP/WSDL = +10, gRPC/Protobuf = +8, WebSocket heavy = +7. More discoverable API = more attack surface = higher rank.' -ForegroundColor Gray"
    },
    {
      "event": "SessionStart",
      "command": "Write-Host '[RANKER] Tech stack Vuln database lookup: for each detected tech, query Vuln DB (NVD + GitHub Advisory + Exploit-DB). Match CVEs with PoC availability. CVEs with public PoC get +5 bonus over CVEs without. Exploit-DB entries = +3. Metasploit modules = +3.' -ForegroundColor Magenta"
    },
    {
      "event": "SessionStart",
      "command": "Write-Host '[RANKER] Target clustering: group targets by (1) tech stack similarity, (2) infrastructure (same CDN, same hosting), (3) codebase similarity (same framework version). Cluster analysis identifies which targets get highest benefit from joint security-reviewer scan.' -ForegroundColor Cyan"
    },
    {
      "event": "SessionStart",
      "command": "Write-Host '[RANKER] Alert fatigue prevention: suppress targets with >=10 existing low-value findings. These targets likely not worth full hunting unless new data suggests otherwise. Suppressed targets ranked but deprioritized to P3 unless new nuclei result.' -ForegroundColor Yellow"
    },
    {
      "event": "PreToolUse",
      "matcher": "Bash",
      "command": "if ($lastCommand -match 'jq.*bug_class|jq.*attack_vector|python.*classify|python.*categorize') { Write-Host '[RANKER PRE] Bug class classification detected. Classifying endpoints: REST/GraphQL/SOAP, numeric IDs present, file ops, auth endpoints, admin panels, query/export endpoints.' -ForegroundColor DarkYellow }"
    },
    {
      "event": "PreToolUse",
      "matcher": "Bash",
      "command": "if ($lastCommand -match 'nvd|NVD|nvd.*nvd|nvd.*api|github.*advisory|advisory.*database') { Write-Host '[RANKER PRE] External Vuln database lookup detected. Matching detected tech stack versions against NVD and GitHub Advisory DB. Results feed tech_stack_match dimension.' -ForegroundColor DarkYellow }"
    },
    {
      "event": "PreToolUse",
      "matcher": "Bash",
      "command": "if ($lastCommand -match 'wayback|waybackurls|commoncrawl|cc-index|urlscan|otx|shodan.*crawler') { Write-Host '[RANKER PRE] Historical URL discovery detected. Found URLs feed feature_age scoring. Wayback URLs older than 3 years = +12 feature_age bonus. Cross-reference with current live endpoints.' -ForegroundColor DarkYellow }"
    },
    {
      "event": "PreToolUse",
      "matcher": "Bash",
      "command": "if ($lastCommand -match 'crt\\.sh|certificate.*transparency|cert.*search|censys.*cert|teams.*cymru') { Write-Host '[RANKER PRE] Certificate transparency log search detected. Results reveal subdomain history, wildcard certificates, and certificate transparency coverage. Missing CT coverage = potential visibility gap.' -ForegroundColor DarkYellow }"
    },
    {
      "event": "PreToolUse",
      "matcher": "Bash",
      "command": "if ($lastCommand -match 'wafw00f|whatwaf|wafdetect|identywaf|waf.*finger') { Write-Host '[RANKER PRE] WAF fingerprinting detected. WAF presence = mature security posture ranking penalty (-5). WAF type documented for p1-warrior WAF bypass planning. WAF bypass adds time cost to estimates.' -ForegroundColor DarkYellow }"
    },
    {
      "event": "PreToolUse",
      "matcher": "Bash",
      "command": "if ($lastCommand -match 'retire|retire\\.js|retire.*-j|retire.*--jshint|check.*vulnerable.*js') { Write-Host '[RANKER PRE] JS vulnerability scanner detected. Checking front-end libraries for known CVEs. Results can shift target ranking if framework found vulnerable. Document found library versions for Vuln DB lookup.' -ForegroundColor DarkYellow }"
    },
    {
      "event": "PreToolUse",
      "matcher": "Bash",
      "command": "if ($lastCommand -match 'nikto|dirbuster|dirb|gobuster|ffuf|feroxbuster|dirsearch|fuzz') { Write-Host '[RANKER PRE] Directory/file brute-force detected. Findings feed into api_surface_score. Brute-force results reveal hidden/admin endpoints. Cross-reference with nuclei templates for Vuln scoring.' -ForegroundColor DarkYellow }"
    },
    {
      "event": "PostToolUse",
      "matcher": "Bash",
      "command": "if ($lastOutput -match 'vulnerable.*version|outdated|EOL|end.of.life|unsupported|deprecated|CVE-[0-9]{4}-[0-9]{4,7}') { Write-Host '[RANKER POST] Outdated/vulnerable version detected. Tech stack match score boosted. CVE references passed to exploit-researcher for PoC retrieval. Outdated components = highest predicted yield.' -ForegroundColor Yellow }"
    },
    {
      "event": "PostToolUse",
      "matcher": "Bash",
      "command": "if ($lastOutput -match 'wp-admin|wp-login|wp-json|xmlrpc\\.php|wp-content|wp-includes') { Write-Host '[RANKER POST] WordPress fingerprint detected. WordPress endpoints = known Vuln patterns. wp-json REST API = unauthenticated enumeration. xmlrpc.php = SSRF/auth bypass. Track for p1-warrior WordPress-specific testing.' -ForegroundColor Yellow }"
    },
    {
      "event": "PostToolUse",
      "matcher": "Bash",
      "command": "if ($lastOutput -match 'Laravel|Symfony|CodeIgniter|CakePHP|Yii|Zend|Phalcon|Slim|FuelPHP|PHPixie') { Write-Host '[RANKER POST] PHP framework detected. Laravel debug mode = immediate +10. Symfony profiler = +8. SQL via raw queries = +5. File upload in PHP = +5. Track framework-specific payloads for hunter.' -ForegroundColor Yellow }"
    },
    {
      "event": "PostToolUse",
      "matcher": "Bash",
      "command": "if ($lastOutput -match 'Django|Flask|FastAPI|Tornado|Bottle|Pyramid|Sanic|Falcon|Hug') { Write-Host '[RANKER POST] Python framework detected. Django debug mode = +10. Flask debug mode = +8. Jinja2 SSTI = +10. PyYAML load = +8. Track Python-specific bug class payloads for hunter.' -ForegroundColor Yellow }"
    },
    {
      "event": "PostToolUse",
      "matcher": "Bash",
      "command": "if ($lastOutput -match 'Spring|SpringBoot|Quarkus|Micronaut|Vert\.x|Play|Jakarta|Hibernate') { Write-Host '[RANKER POST] Java framework detected. Spring Actuator exposed = +10. JNDI lookup (Log4Shell) = +15 if version vulnerable. Deserialization via Jackson = +8. SQL via JdbcTemplate = +5. Track Java-specific payloads for hunter.' -ForegroundColor Yellow }"
    },
    {
      "event": "PostToolUse",
      "matcher": "Bash",
      "command": "if ($lastOutput -match 'Express|Koa|Hapi|Fastify|NestJS|Sails|Feathers|Restify|Adonis') { Write-Host '[RANKER POST] Node.js framework detected. Express apps = prototype pollution risk. JWT common. Check for: helmet usage, rate limiting, CORS config. NestJS = TypeScript = better type safety. Track Node-specific payloads for hunter.' -ForegroundColor Yellow }"
    },
    {
      "event": "PostToolUse",
      "matcher": "Bash",
      "command": "if ($lastOutput -match 'kubernetes|k8s|docker|container|nomad|consul|vault|istio|envoy|service.mesh') { Write-Host '[RANKER POST] Container/orchestration detected. Kubernetes API server exposed = critical. Vault exposed = critical. Docker socket = critical. Service mesh = lateral movement risk. Container targets get infrastructure priority boost +8.' -ForegroundColor Red }"
    },
    {
      "event": "PostToolUse",
      "matcher": "Bash",
      "command": "if ($lastOutput -match 'jenkins|gitlab|github|bitbucket|travis|circleCI|argoCD|spinnaker|tekton') { Write-Host '[RANKER POST] CI/CD system detected. CI/CD = secrets in env vars, build tokens, deployment creds. Access to CI/CD = entire supply chain compromise. +10 priority. Flag for security-reviewer + p1-warrior.' -ForegroundColor Red }"
    },
    {
      "event": "PostToolUse",
      "matcher": "Bash",
      "command": "if ($lastOutput -match 'elastic|kibana|grafana|prometheus|jaeger|zipkin|opentelemetry|APM|NewRelic|Datadog') { Write-Host '[RANKER POST] Observability stack detected. Grafana default admin/admin = +15. Kibana = +10. /metrics endpoint = info leak. APM traces may contain sensitive data in spans. Observability tools often overlooked security-wise.' -ForegroundColor Yellow }"
    },
    {
      "event": "PostToolUse",
      "matcher": "Bash",
      "command": "if ($lastOutput -match 'reseller|partner|affiliate|marketplace|merchant|multi.tenant|customer.portal|vendor.portal') { Write-Host '[RANKER POST] Multi-tenant architecture detected. Multi-tenant = IDOR goldmine. Each tenant data isolation boundary = potential IDOR testing location. Cross-tenant data leakage = high bounty. +8 IDOR boost for all endpoints.' -ForegroundColor Yellow }"
    },
    {
      "event": "PostToolUse",
      "matcher": "Bash",
      "command": "if ($lastOutput -match 'openid|oauth2|oidc|saml|sso|keycloak|auth0|okta|cognito|azure.ad|google.identity') { Write-Host '[RANKER POST] Identity/SSO detected. OAuth misconfig = high bounty. redirect_uri manipulation = open redirect chain. Common bugs: redirect_uri not strict, PKCE missing, scope escalation, token leakage via referrer.' -ForegroundColor Yellow }"
    },
    {
      "event": "PostToolUse",
      "matcher": "Bash",
      "command": "if ($lastOutput -match 'stripe|braintree|square|adyen|razorpay|mollie|checkout|payment_intent|billing|subscription|invoice') { Write-Host '[RANKER POST] Payment/checkout stack detected. Payment bugs = highest bounty category. Check: (1) payment amount manipulation, (2) status bypass, (3) webhook signature, (4) coupon/ promo abuse, (5) currency arbitrage. +15 payment bonus.' -ForegroundColor Red }"
    },
    {
      "event": "PostToolUse",
      "matcher": "Bash",
      "command": "if ($lastOutput -match 'mail|smtp|sendgrid|mailgun|ses|postmark|postal|mailhog|mailtrap|debug.*email') { Write-Host '[RANKER POST] Email service detected. Email flows = password reset token leakage, email enumeration, MX injection. Check: (1) password reset token in URL, (2) email enumeration via response timing, (3) email content injection, (4) SMTP open relay.' -ForegroundColor Yellow }"
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Re-ranking trigger conditions: (1) p1-warrior reports new subdomain/endpoint found, (2) nuclei new result on ranked target, (3) exploit-researcher finds new CVE for detected tech, (4) js-analysis finds new API endpoint, (5) security-reviewer finds new trust boundary violation.' -ForegroundColor DarkGray"
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Ranker output contract: output file format strictly defined in output/contract/ranker_output_schema.json. All fields required. Consumers (p1-warrior, chain-builder, etc.) validate against schema before processing. Schema versioning handled in filename.' -ForegroundColor DarkGray"
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Dynamic score adjustment: ranker tracks per-target difficulty_estimate (1-5). Difficult targets get time_estimate multiplier. Easy targets with high score = P1 first. Hard targets deferred if easier P1s exist. Hunter efficiency optimization.' -ForegroundColor DarkGray"
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Novel finding premium: targets with tech stacks that have no existing recon-ranker history get novelty_bonus=+5. Novel targets may have less-tested code paths. Risk/reward: potentially uncaught bugs vs potentially hardened code.' -ForegroundColor DarkGray"
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Competition mode: if multiple hunters work same program, ranker adjusts for non-overlapping target assignment. Hunter A gets targets not assigned to Hunter B. Coordination via state/knowledge/hunter_assignments.json.' -ForegroundColor DarkGray"
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Program bounty history integration: if program has bounty history in state/programs/{name}/bounty_history.json, weight targets by historical payout. High-yield targets boost priority. Low/zero-yield targets deprioritized even if Vuln-rich.' -ForegroundColor DarkGray"
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Temporal decay: recon data decays over time. Live host data from >72h ago = -3 confidence tier. Tech stack from >7 days = revert to safe assumption. Nuclei from >7 days = rerun. Fresh data always preferred over cached data.' -ForegroundColor DarkGray"
    },
    {
      "event": "Stop",
      "command": "Write-Host '[RANKER STOP] Ranking complete. Audit: input_hash={hash}, targets_ranked={count}, p1_assigned={count}, p2_assigned={count}, p3_assigned={count}, info_assigned={count}, confidence_high={count}, confidence_medium={count}, confidence_low={count}, dau_tier={tier}.' -ForegroundColor Yellow"
    },
    {
      "event": "Stop",
      "command": "Write-Host '[RANKER STOP] Output files written: ranked_targets.json (canonical), ranked_targets_backup_{timestamp}.json (backup), ranker_audit.jsonl (audit), confidence_report.md (summary). All files signed with checksum in output/checksums.txt.' -ForegroundColor Green"
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Automated re-ranking triggers: (1) new nuclei result on ranked target = immediate re-rank, (2) new CVE for detected tech = re-rank affected targets, (3) p1-warrior discovers new endpoint = re-rank that target, (4) js-analysis finds new secret = re-rank. Re-ranking is O(n log n) and fast.' -ForegroundColor DarkGray"
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Cross-program intelligence sharing: if same tech stack found across programs, cross-reference findings. Bug class yield from Program A applicable to Program B with same stack. Ranker shares non-sensitive intel via state/knowledge/tech_stack_intel/{framework}.json.' -ForegroundColor DarkGray"
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Statistical confidence intervals: for each target, compute 95% CI on priority_score. Wide CI = uncertain (data quality issues). Narrow CI = high confidence. Hunter should prioritize targets with narrow CI first for efficiency.' -ForegroundColor DarkGray"
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Cluster differentiation: within a cluster of similar targets, identify the least-tested member as primary. Least-tested member gets +3 priority (novel attack surface). Cluster leader (most comprehensive recon) also ranked but secondary.' -ForegroundColor DarkGray"
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Negative signal handling: targets with WAF + recent security audit + mature framework = negative signals reduce priority. Negative signals: WAF (-5), security audit within 6 months (-3), mature framework with recent updates (-2). Negative signals do not drop below P3.' -ForegroundColor DarkGray"
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Multi-hunter coordination protocol: each hunter registers assigned target list in state/knowledge/hunter_assignments.json. Hunter A and Hunter B target sets non-overlapping. Hunter completion = targets released back to pool. Hunter failure = targets re-queued for next hunter.' -ForegroundColor DarkGray"
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Program bounty profile integration: load state/programs/{program}/bounty_profile.json which contains: avg_payout_per_severity, historical_findings, hunter_leaderboard, time_to_first_bounty. Profile adjusts target scoring: programs with quick payouts = bump time-sensitive targets.' -ForegroundColor DarkGray"
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Time-box adaptation: ranking estimates time per target based on complexity score. Simple target = 20 min, medium = 40 min, complex = 90 min. Time estimates feed into autopilot phase budget. Targets exceeding budget get deferred in normal mode.' -ForegroundColor DarkGray'
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Alert deduplication: before ranking, normalize URLs (strip ports, www, trailing slashes). Deduplicate across all input files. Duplicate URLs with conflicting data = merged with most recent source winning. Dedup reduces noise in ranking.' -ForegroundColor DarkGray"
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Target posture scoring: independently score target security posture. Posture = (has_WAF, has_CDN_WAF, has_modsecurity, has_rate_limit, has_CAPTCHA, has_security_team, has_bug_bounty_program_active). Mature posture = targets harder to hack = lower priority for hunters seeking quick wins.' -ForegroundColor DarkGray"
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Outcome tracking: tie ranking decisions back to actual findings via audit.jsonl. Correlation: did top P1 targets yield bugs? If not, adjust weighting. Feedback loop: ranking algorithm improvements from actual hunter win rates. Measured by: P1 yield rate, time-to-first-bug, false positive rate of ranked targets.' -ForegroundColor DarkGray"
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Integration with network-analyst: if network-analyst captures suspicious traffic patterns, flag related hosts/IPs in ranking for security-reviewer. Network anomalies + ranked target = suspicious activity warranting deep source review.' -ForegroundColor DarkGray'
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Integration with redteam-planner: ranker provides ranked target list to redteam-planner for campaign design. Redteam-planner maps attack paths: initial access target -> pivot target -> crown jewel. Ranker inputs prioritize which target gets initial access treatment.' -ForegroundColor DarkGray'
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Integration with reverse-engineer: if binary/firmware analysis is in scope, ranker includes firmware endpoints/binary targets in ranking. Firmware-only targets get separate ranking track with specialized scoring (protocol reverse engineering difficulty).' -ForegroundColor DarkGray'
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] CISA KEV integration: cross-reference ranked targets against CISA Known Exploited Vulnerabilities catalog. KEV match = +10 priority (known active exploitation). CISA KEV also feeds exploit-researcher for PoC retrieval. KEV = prioritize before next CISA advisory update.' -ForegroundColor DarkGray'
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Threat actor TTP correlation: if threat actor TTPs relevant to target\'s sector/geography are known (MITRE ATT&CK), rank targets facing those TTPs higher. Sector-specific TTP database in state/knowledge/threat_intel/ttp_sector_map.json.' -ForegroundColor DarkGray'
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Ranking algorithm versioning: current algorithm version tagged in output. Algorithm changes require A/B test against historical rankings. Algorithm v1 baseline stored in state/algorithms/baseline_v1.json. New algorithm v2 tested against same data, compared for yield improvement.' -ForegroundColor DarkGray'
    },
    {
      "event": "PromptSend",
      "command": "Write-Host '[RANKER CTX] Fallback ranking: if recon data severely limited (only domain, no subdomain scan), use fallback ranking based on (1) TLD reputation (.gov=high, .com=medium, .xyz=low), (2) known tech stack from passive sources, (3) similar target historical data. Fallback ranking marked with confidence=low.' -ForegroundColor DarkGray'
    },
    {
      "event": "Stop",
      "command": "Write-Host '[RANKER STOP] Ranking complete. Audit: input_hash={hash}, targets_ranked={count}, p1_assigned={count}, p2_assigned={count}, p3_assigned={count}, info_assigned={count}, confidence_high={count}, confidence_medium={count}, confidence_low={count}, dau_tier={tier}.' -ForegroundColor Yellow"
    },
    {
      "event": "Stop",
      "command": "Write-Host '[RANKER STOP] Output files written: ranked_targets.json (canonical), ranked_targets_backup_{timestamp}.json (backup), ranker_audit.jsonl (audit), confidence_report.md (summary). All files signed with checksum in output/checksums.txt.' -ForegroundColor Green"
    }
  ]
}
